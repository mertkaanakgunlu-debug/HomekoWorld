<Window x:Class="HomekoWorld.Views.CudaDownloadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NVIDIA CUDA &amp; TensorRT Eksik" Height="250" Width="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#1E1E1E" Foreground="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="NVIDIA CUDA / TensorRT Kütüphaneleri İndiriliyor" FontWeight="Bold" FontSize="16" Foreground="#FFD700" HorizontalAlignment="Center" Margin="0,0,0,15"/>
        
        <TextBlock x:Name="StatusText" Text="İndirme başlatılıyor..." TextWrapping="Wrap" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,10"/>
        
        <ProgressBar x:Name="DownloadProgress" Height="20" Grid.Row="2" Minimum="0" Maximum="100"/>
    </Grid>
</Window>
